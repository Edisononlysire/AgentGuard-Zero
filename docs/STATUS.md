# 当前状态与已知问题

更新日期：2026-09-07。此页是当前实现的状态清单，不是一份已完成新协议训练的声明。

## 1. 已有实现与最新规划分开

已有结果来自 Qwen3.5-4B LoRA 候选策略、冻结场景生成器与流程约束教师；ECRG 未启用。实现与模型结构见 [ARCHITECTURE](ARCHITECTURE.md)，数值与失败门禁见 [RESULTS](RESULTS.md)。

[PLAN](PLAN.md) 是唯一有效的新方案。成本敏感主动试探、新公共信息教师、干预式证据机制与新评价仍需实现和验证。当前 T12 源码作为可检查的基础保留，不能仅因仓库整理就改称这些功能已经完成。

## 2. 最新服务器审查

本次审查实际导入结果产生路径并进行 CPU 测试和原动作回放，没有重新推理、训练或更改服务器任务。

| 检查 | 结果 |
|---|---:|
| 服务器定向单元测试 | 38 项通过，不是全仓测试 |
| 旧 T12 配对场景 | 300 条隐藏世界轨迹、150 个 public groups |
| 公开状态与存档摘要一致 | 811/811 |
| 逐世界动作执行结果一致 | 1,305/1,305 |
| 核对的终局指标一致 | 300/300 |
| 旧式 true- 事件 ID / environment 来源 | 初始观测中均为 0 |
| 重建模型公开编码含 required= | T1 424/424；T2 387/387 |

回放一致性证明存档动作可重现，不证明没有任何统计泄漏，也不等于重新运行了模型或验证了 GPU 训练稳定性。

## 3. 正式新实验前需要处理

| 问题 | 证据与处理 |
|---|---|
| 流程要求暗示 | 编码保留 require_*，环境与教师也有流程约束。输入、场景门控、教师标签必须一起修改，不能只删除字段。 |
| 行动效果与 belief 耦合 | 同一 LimitSession、同一目标，仅改变声明意图，缓解效果就不同。物理效果与意图识别应分开。 |
| Betrayal 指标宽松 | 公开信息“质疑所有声明”策略，在最小背叛场景中无工具即可获得检测率 1.0；合法变化也被质疑但旧误报指标为 0。应绑定来源、证据、时间及正常变化误报。 |
| Probe Yield 不是因果收益 | 当前检查后续证据引用，已不是任意工具调用计分，但不能代替有/无试探的净效用比较；确认原判断也可能有价值。 |
| 基线后处理 | 共享旧评测助手仍会改写朴素 Memory/Trust 策略。公平基线应保留自主拒绝；不能将朴素策略代表所有记忆智能体。 |
| 兼容入口 fallback | 旧 Level-1 助手缺失场景时仍可能回退。新正式入口应拒绝缺失协议与场景；不能依赖兼容默认值。 |
| 机制外泛化 | 哈希隔离不等于模板、机制或组合隔离；旧 retention 集不再是新协议最终测试。 |

相关源码：[公开编码](../agentguard_zero/candidate/model.py)、[环境](../agentguard_zero/env/cyber_env_v2.py)、[行动效果](../agentguard_zero/world/hidden_world.py)、[信任指标](../agentguard_zero/evaluation/rq4_trust.py)、[试探指标](../agentguard_zero/env/oracle_v2.py)、[教师标签](../scripts/build_v11_t12_native_v2.py)。

错误目标不缓解、目标相关授权、记忆状态互斥、来源独立性、预算与证据可用时点已有实现及测试；这些部分不需要再次推倒重写。

## 4. 本次发布整理

- 当前目录只保留这一组最新文档；旧设计、旧实验结果页、日志、作业脚本与 VerL 训练运行库移入 Git 历史，不另建一个堆满旧内容的 archive 目录。
- 保留 T12 当前入口的 Python 依赖，包括少数命名较旧的共享评测、状态、指纹与兼容模块。它们不是本轮要重启的旧实验。
- 从混合旧测试中迁移 79 项仍适用的环境契约测试，保留测试方法内容；已退出发布范围的旧作业／训练编排测试不再收集。
- 同步服务器早已存在的延迟 probe 事件索引登记，并增加公开版回归测试。它修复发布差异，不修改服务器已有行为。
- 结果 JSON 原样保留，包括 accepted=false 和全部失败项。原始日志、数据与 checkpoint 仍留在私有实验目录，本次未操作这些目录。
- 清理前快照固定在 `pre-cleanup-20260907`，对应 `cff3771d95bb0d75b2c29556a0464718d101f9a9`。

整理后的发布验证包括全量保留测试、8 个 T1/T2 smoke 场景、6 份文档的本地链接检查、Python 语法及当前入口导入检查。新增快照哈希测试确保历史结果未被整理过程改写。这里不包含 GPU 重训或新版协议效果验证。

后续按“修正信息与评价边界 -> 小规模同预算验证 -> 冻结新数据与正式实验”推进。不保证修正后旧指标上涨，不用更多训练掩盖尚未排除的评分捷径。
